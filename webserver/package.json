{
  "name": "webserver",
  "version": "1.0.0",
  "description": "webserver for FinModels project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c rollup-index.config.js && rollup -c rollup-about.config.js && rollup -c rollup-contact.config.js && rollup -c rollup-404.config.js",
    "start": "rollup -c rollup-index.config.js && rollup -c rollup-about.config.js && rollup -c rollup-contact.config.js && rollup -c rollup-404.config.js",
    "prepublish": "npm run build"
  },
  "author": "nwickham",
  "dependencies": {
    "core-js": "^2.4.1",
    "jquery": "^2.2.3",
    "q": "^1.4.1",
    "react": "^15.0.1",
    "react-dom": "^15.0.1"
  },
  "devDependencies": {
    "babel-preset-es2015-loose-rollup": "^7.0.0",
    "babel-preset-react": "^6.5.0",
    "rollup": "^0.34.0",
    "rollup-plugin-babel": "^2.4.0",
    "rollup-plugin-commonjs": "^2.2.1",
    "rollup-plugin-json": "^2.0.0",
    "rollup-plugin-node-resolve": "^1.5.0",
    "rollup-plugin-replace": "^1.1.0",
    "rollup-plugin-uglify": "^0.3.1"
  }
}
